<Window x:Class="Dziennik.View.EditEndingMarkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:my="clr-namespace:Dziennik.Controls"
        Title="Edytuj ocenę" Height="300" Width="300"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Ocena" Style="{StaticResource textBlockTitleStyle}"/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding MarkInput, Mode=TwoWay, ValidatesOnDataErrors=True}" Style="{StaticResource inputControlStyle}"/>
        
        <my:OkCancelControl Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" CommandOk="{Binding OkCommand}" CommandCancel="{Binding CancelCommand}"/>
    </Grid>
</Window>
